# Local VSCode artifacts
.vscode/

# Local Codex artifacts (can include sensitive env snapshots)
.codex/

# Local runtime logs and run outputs
log/
medagent_system/runtime/examples/cluster_runs/

# Python caches
__pycache__/
.pytest_cache/

# Ensure .gitmodules is always tracked (submodules need it)
!.gitmodules
