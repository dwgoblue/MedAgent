# CI Gates

1. Schema validation for `runtime/schemas/*.json`.
2. Deterministic replay tests on golden synthetic bundles.
3. BioMCP verification harness tests with mocked responses.
4. Causal perturbation invariants (no unsupported critical flips).
